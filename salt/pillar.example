# Example Salt pillar — same IPs on every team pod (isolated 192.168.1.0/24).
# Apply from the admin Kali (Salt master). Pin player_user per jumpbox if needed.

enum_quest:
  files_root: /srv/enum_lab
  lab_domain: vault.lab
  ubuntu_host: vault-web
  win_host: vault-dc
  jumpbox_ip: 192.168.1.7
  ubuntu_ip: 192.168.1.10
  win_ip: 192.168.1.11
  jumpbox_cidr: 192.168.1.0/24
  # player_user: ocig30
